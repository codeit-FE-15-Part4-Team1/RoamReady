import InputExample from '@/shared/components/ui/input/Example';

/**
 * @component InputTestPage
 * @description
 * `Input` 컴포넌트의 다양한 사용 예시를 렌더링하는 테스트용 페이지입니다.
 * 디자인 및 동작 확인을 위해 `InputExample`을 포함합니다.
 */
export default function InputTestPage() {
  return <InputExample />;
}
